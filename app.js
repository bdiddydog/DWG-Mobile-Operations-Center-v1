let vapidPublicKey='';const $=id=>document.getElementById(id);window.addEventListener('load',async()=>{if('serviceWorker'in navigator)await navigator.serviceWorker.register('/sw.js');await loadConfig();await loadWeather();updatePushStatus()});$('refresh').onclick=loadWeather;$('enablePush').onclick=enablePush;$('testPush').onclick=testPush;async function loadConfig(){try{const r=await fetch('/api/config',{cache:'no-store'}),d=await r.json();vapidPublicKey=d.vapidPublicKey||'';if((d.missing||[]).length){$('setup').classList.remove('hidden');$('setup').textContent='Setup required in Netlify: '+d.missing.join(', ')}}catch(e){$('setup').classList.remove('hidden');$('setup').textContent='Netlify backend is not available yet.'}}async function loadWeather(){$('loadState').textContent='Updating…';try{const r=await fetch('/api/weather',{cache:'no-store'}),d=await r.json();if(!r.ok)throw Error(d.error||'Weather request failed');$('forecast').innerHTML=d.locations.map(x=>`<article class="card"><div class="zone">${esc(x.zone)}</div><h3>${esc(x.name)}</h3><div class="current"><div class="temp">${num(x.temperatureF)}°</div><div class="condition">${esc(x.condition)}</div></div><div class="metrics"><div class="metric"><span>Feels like</span><strong>${num(x.feelsLikeF)}°F</strong></div><div class="metric"><span>Rain chance</span><strong>${num(x.precipChance)}%</strong></div><div class="metric"><span>Wind</span><strong>${esc(x.wind)}</strong></div><div class="metric"><span>Gust</span><strong>${num(x.windGustMph)} mph</strong></div></div></article>`).join('');$('alerts').innerHTML=d.alerts.length?d.alerts.map(a=>`<article class="alert"><strong>${esc(a.event)}</strong><p>${esc(a.areaDesc)}</p></article>`).join(''):'<p class="muted">No active Delaware NWS alerts.</p>';$('alertCount').textContent=d.alerts.length;$('updated').textContent=new Date(d.generatedAt).toLocaleString();$('loadState').textContent='Current'}catch(e){$('forecast').innerHTML=`<article class="card">${esc(e.message)}</article>`;$('loadState').textContent='Error'}}async function enablePush(){if(!vapidPublicKey)return alert('Add VAPID_PUBLIC_KEY in Netlify first.');const p=await Notification.requestPermission();if(p!=='granted')return updatePushStatus();const reg=await navigator.serviceWorker.ready;let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64(vapidPublicKey)});const r=await fetch('/api/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(sub)});if(!r.ok)throw Error('Could not save phone subscription');updatePushStatus();alert('DWG phone alerts are enabled.')}async function testPush(){const reg=await navigator.serviceWorker.ready,sub=await reg.pushManager.getSubscription();if(!sub)return alert('Enable phone alerts first.');const r=await fetch('/api/test-push',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(sub)});if(!r.ok)alert('Test notification failed.')}function updatePushStatus(){$('pushStatus').textContent=('Notification'in window&&Notification.permission==='granted')?'Enabled':'Not enabled'}function b64(s){const p='='.repeat((4-s.length%4)%4),v=atob((s+p).replace(/-/g,'+').replace(/_/g,'/'));return Uint8Array.from([...v].map(c=>c.charCodeAt(0)))}function num(v){return Number.isFinite(Number(v))?Math.round(Number(v)):'—'}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
