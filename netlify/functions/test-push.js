const{webpush,configure}=require('./_push-common');exports.handler=async event=>{if(event.httpMethod!=='POST')return{statusCode:405,body:'Method not allowed'};try{configure();const s=JSON.parse(event.body||'{}');await webpush.sendNotification(s,JSON.stringify({title:'DWG test alert',body:'Phone notifications are working. Data Over Drama.',tag:'dwg-test',url:'/'}));return{statusCode:200,body:JSON.stringify({sent:true})}}catch(error){return{statusCode:500,body:JSON.stringify({error:error.message})}}};
