const webpush=require('web-push');function configure(){const pub=process.env.VAPID_PUBLIC_KEY,priv=process.env.VAPID_PRIVATE_KEY,subject=process.env.VAPID_SUBJECT||'mailto:weather@example.com';if(!pub||!priv)throw Error('VAPID keys are not configured.');webpush.setVapidDetails(subject,pub,priv)}function id(s){return Buffer.from(s.endpoint).toString('base64url')}module.exports={webpush,configure,id};
